
function AuthenticatedRoute({props}:any) {

    // if(isUserLoggedIn()){
    //     return <Route {...props}/>
    // } else {
    //     return <Redirect to='/login'/>
    // }
    
}

export default AuthenticatedRoute
